$(document).ready(function(){
    var fields = [
        ['Name', 'name'],
        ['Quantity', 'quantity'],
        ['Card Type', 'cardType'],
        ['Actual Type', 'actualType'],
        ['Level', 'level'],
        ['Design', 'design'],
        ['Series', 'series'],
        ['Expansion', 'expansion']
    ];

    var lastEntry = 0;

    $(".add-sort").click(function(){
        var sortParams = $(".sort-params")

        sortParams.append(``)

    })
});