include _queryControls
extends base

block body
    h1.page-header Find Cards
    form(action="/list/", method="get").query-form
        +_queryControls(card, filter)
        button.btn.btn-action(type="submit" autofocus)
            i.fa.fa-search.icon-margin-r
            span Find Cards
